import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  goalItemContainer: {
    margin: 8,
    borderRadius: 6,
    backgroundColor: "#5e0acc",
  },

  goalItemTextPressed: {
    opacity: 0.5,
  },

  goalItemText: {
    color: "#fff",
    padding: 8,
  },
});
